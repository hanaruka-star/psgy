services:
  nginx:
    image: nginx:1.27-alpine
    container_name: parkinglink-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.parkinglink.docker.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
