// PSgy Cloud Functions
// ParkingLink watchlist / rules-bypass functions were removed.
// Backend team adds booking timeout + rating triggers here.
const { setGlobalOptions } = require("firebase-functions/v2");

setGlobalOptions({ region: "asia-southeast1" });
